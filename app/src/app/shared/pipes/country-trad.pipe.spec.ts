import { CountryTradPipe } from './country-trad.pipe';

describe('CountryTradPipe', () => {
  it('create an instance', () => {
    const pipe = new CountryTradPipe();
    expect(pipe).toBeTruthy();
  });
});
